# =============================================================================
# TN_Agriculture_Schemes_RAG dependencies
# Ranges pin a compatible band (no fully open pins) while still allowing
# patch/minor security updates. Install everything with:
#   pip install -r requirements.txt
# =============================================================================

# --- Runtime (required to run the app) ---
streamlit>=1.35,<2
requests>=2.31,<3
beautifulsoup4>=4.12,<5
lxml>=5.0,<7
pandas>=2.0,<3
python-dotenv>=1.0,<2
tenacity>=8.2,<9
langchain>=0.2,<0.4
langchain-core>=0.2,<0.4
langchain-community>=0.2,<0.4
langchain-openai>=0.1,<0.3
langchain-text-splitters>=0.2,<0.4
langsmith>=0.1,<0.2
neo4j>=5.0,<7
langchain-neo4j>=0.10,<2
tiktoken>=0.7,<1
pydantic>=2.5,<3

# --- Dev / test only (not needed to run the app) ---
pytest>=8.0,<9
pytest-cov>=5.0
pytest-mock>=3.12
pytest-timeout>=2.3
responses>=0.25
playwright>=1.44
pytest-playwright>=0.5
ruff>=0.5
black>=24.4
mypy>=1.10
bandit>=1.7
